# Production environment variables for Vercel deployment
# This file is committed so Vercel picks it up during build.
# The backend API lives on Render — all API calls must go here:
REACT_APP_API_BASE_URL=https://ml-early-disease-prediction-0bzh.onrender.com
